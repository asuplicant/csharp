string[] carros = new string[3];
int i = 0;

for (int i = 0, i < carros.Lenght, i++)
{
    Console.WriteLine($"Digite o nome do carro a ser cadastrado: ");
    carros{i} = Console.ReadLine()!;
}
    Console.WriteLine($"Carros cadastrados: {carros[0]}")